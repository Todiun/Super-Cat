<div class="container-fluid bg-primary-subtle py-4 mb-4 text-center">
  <h2 class="text-dark mb-0">This one is cute. That's for sure.</h2>
</div>

<div class="container">
  <div class="row">
    <div class="col-md-6">
      <% if @cat.photo.attached? %>
        <%= image_tag @cat.photo, class: "img-fluid rounded shadow", alt: @cat.name %>
      <% else %>
        <%= image_tag "default_cat.jpg", class: "img-fluid rounded shadow", alt: "Default cat image" %>
      <% end %>
    </div>
    
    <div class="col-md-6">
      <h2 class="mb-4"><%= @cat.name %></h2>
      <p class="lead">
        So it's a cat. But it's a special cat that's for sure. Look at his frons. Spectacular! 
        So it's a cat. But it's a special cat that's for sure. Look at his frons. Spectacular!
      </p>
      <p class="text-muted">
        <small>Race: <%= @cat.race %></small><br>
        <%= @cat.description %>
      </p>
      
      <div class="mt-4 text-center">
        <%= button_to "Choose this cat", add_to_cart_cat_path(@cat), 
            method: :post, 
            class: "btn btn-primary btn-lg px-4" %>
      </div>
      
      <div class="mt-3 text-center">
        <%= link_to "Back to cats", cats_path, class: "text-muted text-decoration-none" %>
      </div>
    </div>
  </div>
  
  <% if notice %>
    <div class="alert alert-success mt-4"><%= notice %></div>
  <% end %>
</div>

<% if current_user&.admin? %>
  <div class="container mt-4">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">Admin Actions</h5>
        <%= link_to "Edit this cat", edit_cat_path(@cat), class: "btn btn-outline-primary me-2" %>
        <%= button_to "Delete this cat", @cat, method: :delete, class: "btn btn-outline-danger", 
            data: { confirm: "Are you sure you want to delete this cat?" } %>
      </div>
    </div>
  </div>
<% end %>