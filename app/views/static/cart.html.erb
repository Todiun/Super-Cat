<div class="landing-container">
    <p class="question-text">There you go. The last step.</p>
    <% if @tempcart != []%> <%# @tempcart contient les objets du panier, soit online soit offline (voir le controlleur) %>
        <% for item in @tempcart do%>
            <div class="cart-item">
                <h4><%= item.name %></h4>
                <img src="<%=asset_path(item.cat_image)%>" width="60px">
                <p><small><%= item.race%></small></p>
                <p><%= item.description%></p>
                <p> Price: <strong><%= number_to_currency(item.price_cents / 100.0) %></strong></p>
                <%= button_to 'Remove from cart', remove_from_cart_path(item), method: :post, class: "btn btn-primary" %>
            </div>
            ___
        <% end %>
    <% else %>
        <p> There is nothing here </p>
    <%end%>
</div>
<div class="text-center my-3">
    <%# Only show a single checkout button when the cart has items. Use server-side logic to
            determine which items are being purchased; don't rely on `@cat` here (it is undefined in this view).
    %>
    <% if @tempcart.present? %>
        <%= button_to "Acheter avec Stripe", checkout_path, method: :post, data: { turbo: false }, class: "btn btn-success" %>
    <% end %>
</div>

<div class="mt-4">
    <%= link_to "Back to cats", cats_path, class: "text-muted text-decoration-none" %>
</div>